# ── Core numerics ─────────────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.11
matplotlib>=3.7

# ── JAX (CPU) ─────────────────────────────────────────────────────────────────
# jaxlib must be installed alongside jax; the cpu extra handles this automatically.
jax[cpu]>=0.4.20

# ── Qiskit ────────────────────────────────────────────────────────────────────
# All used submodules (qiskit.circuit.library, .synthesis, .quantum_info) are
# part of the main qiskit package since the 1.0 monorepo merge.
qiskit>=1.0

# ── TeNPy ─────────────────────────────────────────────────────────────────────
physics-tenpy>=1.0

# ── MPS → circuit ─────────────────────────────────────────────────────────────
# No PyPI release; install directly from the Qiskit community repo.
mps-to-circuit @ git+https://github.com/qiskit-community/mps-to-circuit.git
